# DocMan Docs Site

This directory contains the Docusaurus site for DocMan project documentation.

## Development

```bash
npm install
npm run start
```

## Build

```bash
npm run build
```

The GitHub Pages workflow builds this directory and publishes the generated `build/` artifact.
